// {{ deck.name }} by {{ deck.user.username }}
// {{ deck.description_md }}
// Created: {{ deck.date_created }}
// Updated: {{ deck.date_updated }}

// Faction: {{ deck.faction.name }}


<HOMEPLANET>
  1 * {{ deck.coreworld.exporturl }}


<COLONY>
  P * core/{{ deck.faction.code }}/ship/colony-ship


<MAIN>
{% for type, slots in deck.full_slots.main_deck %}
  // {{ type }}(s)
{% for slot in slots %}
  {{ slot.mainDeck }} * {{ slot.card.exporturl }}
{% endfor %}

{% endfor %}

<TECH>
{% for type, slots in deck.full_slots.tech_pool %}
  // {{ type }}(s)
{% for slot in slots %}
  {{ slot.techPool }} * {{ slot.card.exporturl }}
{% endfor %}

{% endfor %}
{{ footer|default('') }}